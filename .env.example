# Never commit a real key. The OpenAI SDK reads OPENAI_API_KEY automatically.
OPENAI_API_KEY=replace-me
OPENAI_MODEL=replace-with-a-model-available-to-your-project
AGENT_INSTRUCTIONS=Answer concisely and use tools when they improve correctness.
